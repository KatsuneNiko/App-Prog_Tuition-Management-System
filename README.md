# App-Prog_Tuition Management System
 An application for managing the tuition for both students and faculty members. The application was written with Java, using Javafx as a platform for creating the GUI.
